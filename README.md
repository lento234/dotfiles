# dotfiles
  
  My personal dotfiles (bash, vim)
  
# Installation

Clone the dotfiles:

    ```git clone git@github.com:lento234/dotfiles.git```

Install vim settings:

    ```
    cd dotfiles
    ./vimsetup.sh
    ```
